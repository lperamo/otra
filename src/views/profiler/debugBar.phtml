<?php
declare(strict_types=1);

use config\AllConfig;

if (!function_exists('generateArrows'))
{
  /**
   * @param string $type
   */
  function generateArrows(string $type = 'radio') : void
  {
    $debugBarPosition = isset(AllConfig::$debugConfig) && isset(AllConfig::$debugConfig['barPosition'])
      ? AllConfig::$debugConfig['barPosition']
      : 'top';
    $positions = [
      '▲' => 'top',
      '▶' => 'right',
      '▼' => 'bottom',
      '◀' => 'left'
    ];

    foreach($positions as $arrow => $position)
    {
      if ($type === 'radio')
      {
        ?>
        <input type="radio" id="dbg--toggle-<?= $position ?>" name="dbg--position"
               class="dbg--toggle-direction dbg--toggle-<?= $position ?>"<?php
        if ($debugBarPosition === $position)
          echo ' checked';
        ?>>
        <?php
      } else
      {
        ?>
        <label for="dbg--toggle-<?= $position ?>" class="dbg--toggle dbg--toggle-<?= $position ?>"
               title="Move the debug bar to the <?= $position ?>"><?= $arrow ?></label>
        <?php
      }
    }
  }
}
$this::css(CORE_CSS_PATH . 'partials/devMode/devMode'); ?>
<?php generateArrows(); ?>

<div id="dbg--bar" class="dbg--bar">
  <span class="dbg--help-cursor dbg--white" title="Route">'<?= $this->route ?>'</span>
  <span class="dbg--white dbg--separator">|</span>
  <span class="dbg--help-cursor dbg--white" title="Bundle"><?= $this->bundle ?></span><!--
  --><span class="dbg--white dbg--separator">/</span><!--
  --><span class="dbg--help-cursor dbg--white" title="Module"><?= $this->module ?></span><!--
  --><span class="dbg--white dbg--separator">/</span><!--
  --><span class="dbg--help-cursor dbg--white" title="Controller"><?= $this->controller ?></span><!--
  --><span class="dbg--white dbg--separator">/</span><!--
  --><span class="dbg--help-cursor dbg--white" title="Action"><?= $this->action ?></span><!--
  --><span class="dbg--white dbg--separator">|</span><!--
  --><span class="dbg--help-cursor dbg--white" title="Elapsed time"><?= round(microtime(true) - BEFORE, 3) ?>s</span><!--
  --><span class="dbg--white dbg--separator">|</span><?php
  if (isset($_SESSION['sid'])) {
     ?><span class="dbg--on dbg--help-cursor" title="Is a user logged in?">Connected</span><?php
  } else {
    ?><span class="dbg--off dbg--help-cursor" title="Is a user logged in?"">Disconnected</span><?php
  } ?>
  <span class="dbg--white dbg--separator">|</span>
  <button id="show-sql" class="dbg--btn">Inspect SQL</button>
  <?php generateArrows('label') ?>
</div>
<?php $this::js(CORE_JS_PATH . 'debug_bar');
  $this::js(CORE_JS_PATH . 'lightbox');
