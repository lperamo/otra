#compdef _otra otra

function _otra {
    BASE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
    CORE_DIR=${BASE_DIR}/lib/otra/
    export BASE_DIR
    . ${CORE_DIR}console/shellCompletions/completionHandler.sh
}
